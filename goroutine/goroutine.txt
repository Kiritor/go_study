并发实现模型

         1、多进程：进程间互不影响,系统开销大,由内核管理。
		 2、多线程 : 系统层面的并发模式,效率依然不是非常理想。
	     3、基于回调的非阻塞/异步IO : 通过事件驱动的方式使用异步IO,减少线程降低消耗。Node.js
		 4、协程 : 本质上是一种用户态线程,不需要系统来进行抢占式调用,寄存于线程之中,高效,需要语言的支持。
		 
并发编程的难度在于协调(并发单元间的通信)
		 
并发通信
         
		 1、共享数据 : 内存数据块、磁盘文件、网络数据---共享内存。
		 2、消息